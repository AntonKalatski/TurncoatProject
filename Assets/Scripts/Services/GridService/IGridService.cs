namespace Services.GridService
{
    public interface IGridService
    {
        Grid CreateGrid();//todo levelGridConfig
        // GridCell GetGridCell(Vector3 point);
        // void SetGridCell(Unit selectedUnit, GridCell hitPoint);
    }
}